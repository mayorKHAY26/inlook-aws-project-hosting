version: "3.8"

services:
  grafana:
    image: grafana/grafana
    container_name: inlook-grafana
    ports:
      - "3000:3000"
    volumes:
      - ./dashboards:/var/lib/grafana/dashboards
      - ./provisioning:/etc/grafana/provisioning